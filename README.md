# Claude Code Init

Claude Code 프로젝트를 위한 초기 설정 템플릿. **CLAUDE.md** (프로젝트 지침), **MEMORY.md** (영속적 메모리), **update_notes/** (스킬 그래프 기반 지식 관리)를 한 번에 세팅합니다.

## Quick Start

```bash
# 1. 기존 프로젝트에 적용 (연구용)
git clone https://github.com/crimama/claude_code_init.git /tmp/claude_code_init
bash /tmp/claude_code_init/setup.sh research /path/to/your/project

# 2. 새 프로젝트 시작
mkdir my-project && cd my-project && git init
bash /tmp/claude_code_init/setup.sh research .
```

## Presets

| Preset | 용도 | 추가 기능 |
|--------|------|----------|
| `base` | 범용 (기본값) | CLAUDE.md + MEMORY.md + update_notes/ |
| `research` | ML/DL 연구 | 6단계 실험 프로세스, _lessons.md, Config 태그 |
| `industry-academia` | 산학과제 | 납품물 관리, 회의록, 마일스톤 추적, 데이터 보안 |

```bash
bash setup.sh base              # 범용
bash setup.sh research          # 연구
bash setup.sh industry-academia # 산학과제
```

## System Architecture

이 시스템은 3개 레이어로 구성됩니다:

```
┌─────────────────────────────────────────────────────┐
│  Layer 1: CLAUDE.md (프로젝트 지침)                    │
│  ─ 프로젝트 원칙, 아키텍처, 명령어, 실험 프로세스        │
│  ─ git tracked, repo 안에 위치                        │
│  ─ 매 세션 시스템 프롬프트에 자동 주입                    │
├─────────────────────────────────────────────────────┤
│  Layer 2: MEMORY.md (영속적 메모리)                    │
│  ─ 실험 결과, 교훈, 의사결정, 현재 상태                  │
│  ─ ~/.claude/projects/{path}/memory/ 에 위치          │
│  ─ 세션 간 지속, 시스템 프롬프트에 자동 로드 (200줄)     │
├─────────────────────────────────────────────────────┤
│  Layer 3: update_notes/ (스킬 그래프)                  │
│  ─ experiments/ → analysis/_lessons.md → ideas/       │
│  ─ 노트 간 "## 관련 노트"로 양방향 링크                  │
│  ─ 검증된 패턴은 _lessons.md로 승격                     │
│  ─ git tracked, repo 안에 위치                        │
└─────────────────────────────────────────────────────┘
```

### Layer별 역할 구분

| | CLAUDE.md | MEMORY.md | update_notes/ |
|---|---|---|---|
| **내용** | 규칙/구조 (변하지 않음) | 상태/지식 (계속 갱신) | 상세 기록 (축적) |
| **비유** | 헌법 | 작업 일지 | 논문/보고서 아카이브 |
| **갱신 주기** | 드물게 (구조 변경 시) | 매 세션 | 매 작업 |
| **200줄 제한** | 없음 (전체 로드) | 있음 (핵심만) | 없음 (필요시 참조) |

## 스킬 그래프 (Knowledge Graph)

`update_notes/` 내 노트들이 `## 관련 노트` 섹션을 통해 서로 링크되어 DAG 형태의 지식 그래프를 형성합니다.

```
실험A ──→ analysis/topic_x/_lessons.md ←── 실험B
  │                                          │
  └──→ ideas/new_approach.md ──→ 실험C ──→ analysis/topic_y/_lessons.md
```

### 핵심 메커니즘: 승격 (Promotion)

```
개별 실험 결과 (experiments/report.md)
        │
        │  반복 확인 시
        ▼
검증된 패턴 (analysis/{topic}/_lessons.md)   ← "승격"
        │
        │  핵심 요약
        ▼
프로젝트 메모리 (MEMORY.md)                   ← 매 세션 자동 로드
```

## 6단계 실험 프로세스 (Research / Industry-Academia)

```
1. 문제 분석 (Problem Analysis)  → 현상 + 원인 추정
2. 가설 설정 (Hypothesis)        → "X하면 Y 개선" + 예상 수치
3. 실험 설정 (Experiment Design) → 대조군/실험군 + 실행 커맨드
───── 실험 실행 ─────
4. 결과 (Results)                → 정량 결과표
5. 결과 분석 (Analysis)          → 가설 검증 ✅/❌/⚠️
6. 피드백 (Feedback)             → 교훈 + _lessons.md 승격 여부
```

**규칙**: 실험 시작 전에 1~3단계를 **먼저 작성**한 후 실행. 실험 후 4~6단계 기록.

## 디렉토리 구조

### Base
```
your-project/
├── CLAUDE.md                     # 프로젝트 지침
├── MEMORY_TEMPLATE.md            # MEMORY.md 참조용 사본
├── .claude/
│   └── settings.local.json       # 프로젝트별 권한 설정
└── update_notes/
    ├── experiments/
    │   └── _TEMPLATE.md
    ├── analysis/
    │   └── _LESSONS_TEMPLATE.md
    ├── bugfix/
    └── ideas/
```

### Research (추가)
```
└── update_notes/
    └── experiments/
        └── _TEMPLATE.md          # 6단계 프로세스 + config_diff
```

### Industry-Academia (추가)
```
your-project/
├── data/
│   ├── public/                   # 공개 데이터
│   └── proprietary/              # 기업 데이터 (gitignore)
├── demo/                         # 기업 데모용
├── reports/                      # 자동 생성 리포트
├── .gitignore                    # proprietary 데이터 보호
└── update_notes/
    ├── deliverables/             # 📦 납품물 관리
    │   └── _TEMPLATE.md
    └── meetings/                 # 📋 회의록
        └── _TEMPLATE.md
```

## Setup 후 할 일

1. **`CLAUDE.md` 편집** — `<!-- -->` 주석 부분을 프로젝트에 맞게 채우기
2. **`MEMORY.md` 편집** — `~/.claude/projects/{path}/memory/MEMORY.md` 초기 내용 작성
3. **Claude Code 시작** — 해당 디렉토리에서 `claude` 실행
4. **(선택) `settings.local.json` 편집** — 자주 쓰는 bash 명령 자동 허용 추가

## MEMORY.md 위치 참고

Claude Code는 프로젝트 경로를 해시하여 메모리 디렉토리를 결정합니다:
```
~/.claude/projects/-{path-with-dashes}/memory/MEMORY.md
```

예: `/home/user/projects/my-research` → `~/.claude/projects/-home-user-projects-my-research/memory/MEMORY.md`

`setup.sh`가 이 경로를 자동으로 설정합니다.

## License

MIT
